#ifndef LONGEST_PUSH_TIME_H
#define LONGEST_PUSH_TIME_H

#include <vector>
#include <string>

class Solution {
public:
    char slowestKey(const std::vector<int>& releaseTimes, const std::string& keysPressed);
};

#endif // LONGEST_PUSH_TIME_H